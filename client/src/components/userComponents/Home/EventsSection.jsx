

import { FiChevronRight } from 'react-icons/fi';

const EventsSection = () => {
  // Upcoming concerts are currently disabled to avoid confusion.
  // We can restore this section later once the booking links are ready.
  return null;
};

export default EventsSection;
